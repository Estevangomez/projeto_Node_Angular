import { async, TestBed } from '@angular/core/testing';
import { PostoTipoArquivoComponent } from './posto-tipo-arquivo.component';
describe('PostoTipoArquivoComponent', function () {
    var component;
    var fixture;
    beforeEach(async(function () {
        TestBed.configureTestingModule({
            declarations: [PostoTipoArquivoComponent]
        })
            .compileComponents();
    }));
    beforeEach(function () {
        fixture = TestBed.createComponent(PostoTipoArquivoComponent);
        component = fixture.componentInstance;
        fixture.detectChanges();
    });
    it('should create', function () {
        expect(component).toBeTruthy();
    });
});
//# sourceMappingURL=posto-tipo-arquivo.component.spec.js.map