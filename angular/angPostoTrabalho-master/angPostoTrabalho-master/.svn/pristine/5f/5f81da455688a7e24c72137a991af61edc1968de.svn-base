import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { PostoTipoArquivoComponent } from './posto-tipo-arquivo.component';

describe('PostoTipoArquivoComponent', () => {
  let component: PostoTipoArquivoComponent;
  let fixture: ComponentFixture<PostoTipoArquivoComponent>;

  beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ PostoTipoArquivoComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(PostoTipoArquivoComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
